{
  "name": "daily-event-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Shared daily event management app with login.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
